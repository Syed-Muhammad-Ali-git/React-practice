# Deployment TODO List

- [ ] Initialize Git repository in the project directory.
- [ ] Add the GitHub repo as remote origin.
- [ ] Commit all current files and push to the `main` branch.
- [ ] Install `gh-pages` as a dev dependency.
- [ ] Update `package.json` to add `homepage` URL and a `deploy` script.
- [ ] Build the project using `npm run build`.
- [ ] Deploy to GitHub Pages using `npm run deploy` (pushes `dist/` to `gh-pages` branch).
